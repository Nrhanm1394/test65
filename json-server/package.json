{
  "name": "json-server",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "@material-ui/core": "^4.11.4",
    "json-server": "^0.16.1"
  }
}
